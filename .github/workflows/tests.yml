name: test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: leafo/gh-actions-lua@v5
      with:
        luaVersion: "5.3"
    - name: test
      run: |
        lua -v
        cd src; lua about.lua -g all
